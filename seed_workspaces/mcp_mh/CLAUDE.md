# MCP-Atlas Harness Workspace

This workspace contains an AI agent's harness for the MCP-Atlas benchmark.
Use the `/harness-optimizer` skill for full instructions on diagnosing
failures and proposing improvements.

Key files: `prompts/system.md`, `harness.py`, `skills/`, `tools/`, `memory/`.
Read-only archive: `evolution/candidates/` (prior candidates with code, scores, traces).
